{
  "name": "mekit-car-rentals",
  "version": "2.0.0",
  "private": true,
  "description": "Mekit Car Rentals — premium vehicle listing site with WhatsApp booking",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "npm install --prefix frontend && cd frontend && npx next build",
    "start": "cd frontend && npm run start"
  }
}
